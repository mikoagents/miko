# Cursor Grok 4.7 model selection regression

**Date:** 2026-09-24
**Repository:** `/tmp/atmiko-cursor-pr-f1-20260924`
**Branch:** `fix/cursor-grok-47`, based on `origin/main`

## F1 results

- Initialized a fresh F1 repository and isolated service on port 3600.
- `ping` and `status` passed; created `issue-1` / `DEF-1` and started `session-1`.
- Description selected `[agent=cursor] [model=cursor-grok-4.7-xhigh-fast]` and
  requested only the README title, without edits, commits or PRs.
- The real Cursor SDK accepted the selection and completed the request.
- Paginated activity inspection returned seven timestamped records: routing,
  model selection, thoughts, a Bash action and the final response.
- The initial file-read tool was denied by existing permissions; the runner used
  an allowed working-directory command and correctly returned **Simple Rate Limiter**.
- Board snapshot reported `model: grok-4.7`, `reasoningEffort: xhigh`,
  `fastMode: true`, and `status: completed`.
- Stopped the session through F1 and shut down the service cleanly.

## Automated checks

- Cursor suite: 62 tests passed, including explicit selectors, catalog defaults,
  create/resume mapping and recorded reasoning effort.
- Full package suite: 2,025 passing tests, two skipped.
- Full build, workspace typecheck and lint passed (10 existing lint warnings).
- No dependency changes; frozen-lockfile install passed.

This validates actual provider acceptance and activity reporting for an explicit
Grok 4.7 selector. The catalog-default path is covered by adapter tests; no external
issue or remote PR was created by the test.
